# ClippyCat v2.6.1 updater proof result

- Starting SHA: `a32785d2b9e62398e91ce768fd2cd14406636f3b`

## Files changed

- `MarmaladeDesktopPet.csproj`
- `Program.cs`
- `README.md`
- `installer/ClippyCat.iss`
- `codex-checkpoints/v2.6.1-updater-proof-plan.md`
- `codex-checkpoints/v2.6.1-updater-proof-result.md`

Generated publish and installer artifacts remain ignored and are not part of the source commit.

## Version locations updated

- Authoritative project/package version: `2.6.1`.
- Generated executable product version: `2.6.1` (`2.6.1.0` file version).
- About dialog: `Version 2.6.1`, using the existing three-part runtime version formatter.
- Inno Setup direct-build fallback: `2.6.1`.
- Installer and checksum filename examples in the README: `2.6.1`.
- `Build-Installer.ps1` required no edit because it already derives the installer version and filename from the project version.

## Visible About change

- Added the concise line `Updater validation release` beneath `Version 2.6.1`.

## Importer validation

- First explicit run: success; `0` atlases changed, with no backup or write for either pet.
- Second explicit run: success; `0` atlases changed, with no backup or write for either pet.
- The importer invocation performed during publish/installer packaging was also unchanged and created no backup or atlas write.

## Build and publish

- `dotnet build -c Release`: success, `0` warnings, `0` errors.
- `Publish-ClippyCat.ps1`: success; self-contained Windows x64 payload contains 468 files totaling 172.41 MiB.
- Published executable product version: `2.6.1`.

## Installer

- `Build-Installer.ps1`: success.
- Installer: `artifacts\installer\ClippyCatSetup-2.6.1.exe`.
- Installer size: 64,093,550 bytes (61.12 MiB).
- SHA-256: `46D0593B1D0545D78D9B48571AE4B01DD6092E7D8777A0731C3A593C37581ABA`.
- Checksum file: `artifacts\installer\ClippyCatSetup-2.6.1.exe.sha256`.
- An independent `Get-FileHash -Algorithm SHA256` calculation matches the checksum file exactly.
- Inno Setup emitted its existing non-blocking deprecation warning for the `x64` architecture identifier; compilation completed successfully. Changing that unrelated installer setting is outside this proof release.

## GitHub Release contract

- Tag: `v2.6.1`.
- Installer asset: `ClippyCatSetup-2.6.1.exe`.
- Checksum asset: `ClippyCatSetup-2.6.1.exe.sha256`.
- Release must have `draft=false` and `prerelease=false`.
- `2.6.1` compares newer than `2.6.0`.
- The existing v2.6.0 updater parses this three-part tag, rejects drafts/prereleases, derives the two exact filenames above, and requires one matching asset of each type. No updater-code change is required.

## Remaining manual live test steps

1. On a machine with the public v2.6.0 installer installed, open About and confirm it reports `Version 2.6.0` before the test.
2. After this source commit is pushed, create a non-draft, non-prerelease GitHub Release tagged `v2.6.1`; upload exactly the generated installer and checksum assets listed above.
3. Start installed v2.6.0 or choose **Check for Updates...** from its tray menu.
4. Confirm the update dialog reports installed `2.6.0` and available `2.6.1` without an error.
5. Choose **Download and Install**; confirm checksum verification succeeds and Windows opens the installer.
6. Complete the per-user upgrade, relaunch ClippyCat, and confirm About shows `Version 2.6.1` plus `Updater validation release`.
7. Confirm existing pet choice, size, always-on-top, startup, and other saved settings remain intact.
8. Check for updates again and confirm v2.6.1 does not offer itself as a newer release.
