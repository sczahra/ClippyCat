# v1.7 Dedicated-Art Preparation Result

## Base commit

`27a99faf1d23912be1799c8c956c01078f777943`

## Files changed

- `Program.cs`
- `README.md`
- `codex-checkpoints/v1.7-dedicated-art-prep-plan.md`
- `codex-checkpoints/v1.7-dedicated-art-prep-result.md`

No sprite assets, diagnostics files, settings code, project files, or dependencies changed.

## Reused animation logic removed

- Stretch no longer reuses the Pawing row.
- Scratch no longer reuses the Grooming row.
- Removed the borrowed-animation `EnterStretch` and `EnterScratch` state-entry methods.
- Removed the action-specific 1.8-2.7 second / 280 ms Stretch timing and 1.6-2.4 second / 150 ms Scratch timing.
- Removed `PetState.Stretch` and `PetState.Scratch` because they had no valid animation state to represent without dedicated artwork.
- Removed their temporary energy-update cases.
- No unrelated animation state is entered when either command is selected.

## Action plumbing preserved

- `PetAction.Stretch` and `PetAction.Scratch` remain available for future dedicated implementations.
- Direct **Stretch** and **Scratch** tray menu items remain in place.
- Both commands continue through the shared `TriggerAction(PetAction)` and `PerformPetAction` dispatcher path.
- The dispatcher retains explicit cases for both actions, making the placeholder easy to replace with dedicated state-entry methods later.

## Tray behavior

- Selecting **Stretch** shows: `Stretch animation coming soon.`
- Selecting **Scratch** shows: `Scratch animation coming soon.`
- The message is informational, does not crash, and does not change the pet to an unrelated animation state.

## Random and autonomous behavior

- **Do Something** is restored to its original five implemented actions: Wave, Jump, Groom, Paw, and Review.
- Stretch and Scratch are not randomly selected, so placeholder messages never appear spontaneously.
- Autonomous selection no longer returns Stretch or Scratch.
- Normal-energy Idle probability is restored to 34%; high-energy Idle probability is restored to 12%.
- All other pre-v1.7 action thresholds remain unchanged.

## Documentation and version

- About remains **Version 1.7** and now describes the commands as prepared for dedicated animation artwork.
- README documents that both commands are present while their dedicated animation artwork is pending.

## Build result

Command: `dotnet build`

Result: **Succeeded** with **0 warnings and 0 errors**.

Output: `bin/Debug/net8.0-windows/MarmaladeDesktopPet.dll`

## Exact next step for dedicated sprite rows

Create and validate dedicated Stretch and Scratch animation rows for both Marmalade and Merry with matching row indexes and frame counts. After the atlas contract is established, add the row/frame constants, restore only the two corresponding `PetState` values and state-entry methods with timing based on the real artwork, replace the two dispatcher placeholders with those methods, and then deliberately re-enable each action in **Do Something** and autonomous weighting after visual runtime testing.
