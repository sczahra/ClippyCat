# ClippyCat v2.5 Standalone Windows Packaging — Result

## Baseline

- Starting commit: `d8b1bf2ad3eb676bd950dad731360d619ca23c0c`
- Starting branch: `main`
- Starting working tree: clean

## Files changed

- `.gitignore`
- `MarmaladeDesktopPet.csproj`
- `Program.cs`
- `DiagnosticsBootstrap.cs`
- `README.md`
- `Publish-ClippyCat.ps1`
- `Build-Installer.ps1`
- `installer/ClippyCat.iss`
- `codex-checkpoints/v2.5-packaging-plan.md`
- `codex-checkpoints/v2.5-packaging-result.md`

Generated `artifacts/` output is ignored and is not part of the source commit.

## .NET environment

- SDK: 8.0.425
- MSBuild: 17.11.48
- Host: Windows 10.0.26100, x64
- Self-contained runtime restored/published: .NET 8.0.31 for `win-x64`
- The host's user NuGet configuration contained no sources. The required official Microsoft runtime packs were restored from `https://api.nuget.org/v3/index.json`; no application package reference was added.
- `Publish-ClippyCat.ps1` specifies that official source so release publishing is reproducible on a machine whose user source list is empty.

## Inno Setup status

- `ISCC.exe` was not found on PATH.
- It was also absent from:
  - `C:\Program Files (x86)\Inno Setup 6\ISCC.exe`
  - `C:\Program Files\Inno Setup 6\ISCC.exe`
  - checked legacy Inno Setup 5 paths during initial tool discovery
- No packaging software was installed.
- `Build-Installer.ps1` was run and behaved as designed: it produced a fresh validated standalone publish, reported the missing compiler and checked paths, and stopped without attempting installation or creating a misleading Setup executable.

## Product identity and executable decision

- Product/display name: ClippyCat
- Publisher: ClippyCat Project
- Product version: 2.5.0
- File version: 2.5.0.0
- Windows file description: ClippyCat
- About dialog: `ClippyCat`, `Version 2.5`
- Diagnostics session version: 2.5
- Internal executable remains `MarmaladeDesktopPet.exe` to avoid unnecessary namespace, mutex, startup, and compatibility risk. End-user installer, shortcuts, uninstall entry, file metadata, and About branding use ClippyCat.

## Publish configuration

- Framework: .NET 8 WinForms
- Configuration/RID: Release, `win-x64`
- Deployment: self-contained folder
- Single-file: disabled
- Trimming: disabled
- Release debug symbols: disabled
- Publish output: `artifacts\publish\win-x64`
- Executable: `artifacts\publish\win-x64\MarmaladeDesktopPet.exe`
- Payload: 466 files, 180,348,054 bytes (171.99 MiB)

The project content list was narrowed to the runtime requirements:

- `Assets\Marmalade\spritesheet.png`
- `Assets\Merry\spritesheet.png`
- `Assets\actions\actions.json`

The published tree contains no development action-frame PNGs, PowerShell scripts, C# source, Git data, checkpoints, backup atlases, or legacy source assets.

## Publish script behavior

`Publish-ClippyCat.ps1`:

1. Resolves and validates the project root.
2. Verifies that its exact clean target stays under `artifacts\publish`.
3. Removes only the generated `win-x64` publish directory.
4. Runs the existing action importer.
5. Runs self-contained `dotnet publish` with conservative WinForms settings.
6. Validates the executable, Assets directory, manifest, both production atlases, and absence of development PNG/script content.
7. Prints the publish path, file count, and total size.

The script is safe to rerun and completed successfully on repeated runs.

## Installer configuration

- Definition: `installer\ClippyCat.iss`
- Intended output: `artifacts\installer\ClippyCatSetup-2.5.0.exe`
- Actual installer output: not compiled because Inno Setup 6 is unavailable
- Install scope: per-user, no elevation
- Default location: `%LOCALAPPDATA%\Programs\ClippyCat`
- Architecture: Windows x64
- Includes the entire self-contained publish folder and excludes debug symbols
- Start Menu shortcut: ClippyCat
- Desktop shortcut: optional, unchecked installer task
- Add/Remove Programs metadata and uninstaller: configured
- Post-install launch: direct `MarmaladeDesktopPet.exe`; no PowerShell window or script
- Uninstall removes installed application files but intentionally does not delete LocalAppData preferences or diagnostics

Per-user installation was chosen because settings, diagnostics, and startup registration are already per-user. Program Files elevation would add complexity without improving runtime safety.

## Installed data and startup behavior

- Settings: `%LOCALAPPDATA%\MarmaladeDesktopPet\settings.json`
- Diagnostics: `%LOCALAPPDATA%\MarmaladeDesktopPet\diagnostics.log`
- Optional startup: `HKCU\Software\Microsoft\Windows\CurrentVersion\Run`, value `ClippyCat`
- Startup command uses the quoted `Application.ExecutablePath`, so an installed copy records its actual installed executable rather than a development build path.
- Single instance remains protected by `Local\ClippyCat.MarmaladeDesktopPet.SingleInstance` and has no repository dependency.
- No existing installation was found at `%LOCALAPPDATA%\Programs\ClippyCat` or in the inspected uninstall registry entries; nothing was installed or uninstalled during validation.

## Validation results

- Action importer run 1: 0 atlas writes, 0 backups.
- Action importer run 2: 0 atlas writes, 0 backups.
- Final `dotnet build -c Release`: succeeded with 0 errors and 0 warnings.
- Final `Publish-ClippyCat.ps1`: succeeded.
- Required executable, runtime dependencies, Assets directory, manifest, Marmalade atlas, and Merry atlas: present.
- Runtime asset set: exactly the manifest plus two production atlases.
- Published metadata: ClippyCat / 2.5.0 / ClippyCat Project.
- Published executable launch: succeeded directly from `artifacts\publish\win-x64`.
- Process path matched the published executable and stayed running during the smoke window.
- Persisted settings loaded: active pet was Merry.
- Diagnostics reported .NET 8.0.31 and wrote under LocalAppData.
- Merry production atlas loaded at 1536x4784, confirming application-relative runtime asset resolution.
- The test process was stopped after validation; no startup registry or user settings were changed.
- `git diff --check`: passed with only normal working-copy line-ending notices.

## Runtime safety review

- Ordinary app startup does not call Git, PowerShell, the importer, or any repository script.
- Writable settings and diagnostics remain outside the installed application directory.
- Runtime uses only generated production atlases and `actions.json`; development action source frames are not required or shipped.
- Existing development workflow remains available through `Build-And-Run-Actions.ps1`.

## Manual tests remaining

Inno Setup compilation and install/uninstall testing require Inno Setup 6 and were not possible on this host. After the compiler is installed separately:

1. Run `.\Build-Installer.ps1`.
2. Confirm `artifacts\installer\ClippyCatSetup-2.5.0.exe` is created and digitally inspect its metadata/content list.
3. If no user installation exists, install normally and verify the Start Menu shortcut, optional Desktop shortcut, Installed Apps entry, direct launch, both pets, tray actions, settings persistence, and Start with Windows path.
4. Uninstall and confirm installed program files are removed while `%LOCALAPPDATA%\MarmaladeDesktopPet` remains.
5. Reinstall and confirm preserved settings load.

The layered pet rendering and complete tray interaction were not automated; they require a short visual desktop-session check. Process startup, persisted pet selection, diagnostics, and atlas loading were verified.

## Exact release command

With Inno Setup 6 available:

```powershell
.\Build-Installer.ps1
```

For a standalone folder release without compiling the installer:

```powershell
.\Publish-ClippyCat.ps1
```
